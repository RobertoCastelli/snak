// CONSTANTS
const TITLE = 'SNAK!'

const LENGTH_X = 20
const LENGTH_Y = 20

const DIRECTIONS = {
	UP: { x: 0, y: -1 },
	DOWN: { x: 0, y: 1 },
	LEFT: { x: -1, y: 0 },
	RIGHT: { x: 1, y: 0 },
}

export { TITLE, LENGTH_X, LENGTH_Y, DIRECTIONS }
